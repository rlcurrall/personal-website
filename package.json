{
  "name": "rc-website",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "lerna bootstrap && lerna run build",
    "start": "lerna run start"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
