{
  "name": "rc-website",
  "version": "2.0.0",
  "private": true,
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "heroku-prebuild": "lerna bootstrap",
    "build": "lerna run build",
    "start": "lerna run start"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
